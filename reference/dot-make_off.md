# QPAD offsets, wrapped by the `wt_qpad_offsets` function.

Functions to get the offsets.

## Usage

``` r
.make_off(spp, x)
```

## Arguments

- spp:

  species for offset calculation.

- x:

  Dataframe out from the `.make_x` function.
