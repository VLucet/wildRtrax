# Switch locale to another language

Global function to allow a user to request data in another language.
Currently English = en or French = fr.

## Usage

``` r
.language(language = c("en", "fr"))
```
